package com.evgeny.testdigitalnomads.repository.network

import com.evgeny.testdigitalnomads.repository.network.MainApi


class MainApiHelper constructor(private val mainApi: MainApi) {


/*
    suspend fun getBids(request: GetBids) = mainApi.getBids(
        id = toMultipartFormData(pref.userID),
        params = toMultipartFormData(request.toJson())
    )
*/



}